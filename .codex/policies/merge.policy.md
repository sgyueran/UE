# Merge Policy

- Task Pull Requests target `develop`.
- `main` is release-ready only.
- Prefer Squash Merge.
- Never force-push.
- Codex may create a draft Pull Request but must not self-merge.
- One task should produce one reviewable Pull Request.
- After merge, recover state before preparing the next task.
